part of 'base_durations_ext.dart';

abstract final class _BaseDurations {
  // Animation durations
  static const Duration animSwitchPrim = Duration(milliseconds: 150);
  static const Duration animScrollToTop = Duration(milliseconds: 300);

  // Search field durations
  static const Duration searchTrans = Duration(milliseconds: 700);
}
