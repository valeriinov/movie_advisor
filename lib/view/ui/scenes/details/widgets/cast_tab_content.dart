import 'package:flutter/material.dart';

class CastTabContent extends StatelessWidget {
  const CastTabContent({super.key});

  @override
  Widget build(BuildContext context) {
    return SliverToBoxAdapter();
  }
}
